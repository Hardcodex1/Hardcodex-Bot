//const { Guild } = require("discord.js");

//module.exports = (bot, triggerText, replyText) => {
  //  bot.on('message', (message) => {
   //     var words = message.content.split(" ");
    //    for (var i = 0; i <= words.length; i++)
    //    {
    //        temp = words[i];
   //             if (temp === 'bbbbb' || temp === 'abdul' || temp === 'tic tok' || temp === 'nigga' || temp === 'go die') 
    //            {
    //                //message.author.send(`Your Message ${message} has been deleted from ${Guild.name}`)
     //               message.author.send(`Your Message has been deleted from A Server, Be Careful`);
      //              message.delete();
      //          }
      //  }
   // })
 // }